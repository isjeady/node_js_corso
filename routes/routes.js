const API = '/api';

//AUTH
const AUTH = '/auth';
const REGISTER = '/register';
const LOGIN = '/login';
const ME = '/me';

//POSTS
const POSTS = '/posts';
const POSTS_SEARCH = '/search';
const POSTS_LIKE = '/like';

const GALLERY = '/gallery';

const USER = '/user';

export { 
    API, 
    POSTS, 
    AUTH, 
    REGISTER, 
    LOGIN, 
    ME,
    POSTS_SEARCH,
    POSTS_LIKE,
    GALLERY,
    USER 
};